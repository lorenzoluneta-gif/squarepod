# Apple MusicKit Android SDK

Place the official Apple MusicKit Android SDK AAR files in this directory.

The app uses reflection for the bridge, so it can still compile without the SDK.
Real Apple Music sign-in requires the SDK to be present at runtime.
